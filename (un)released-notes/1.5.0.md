### New Features
- Added support for gift events (currently exclusive to YouTube):
  - Default widget now shows gift alerts;
  - Emulator can dispatch gift events for testing;
- Implemented globally emulator events dispatch (to receive events emitted by the emulator in your OBS source for example);
  - All events dispatched by the emulator has a flag `unichat:emulator_generated` to identify them as emulator-generated events;
- Added warn messages to widgets;
- Added support for custom icons in scrapers (only `data:` URLs);
- Added MSI installer for Windows;
- Added currency conversion for donations, powered by [frankfurter.dev](https://frankfurter.dev/):
  - Pick a target currency under `Settings > General`, it is disabled by default;
  - YouTube Super Chats and Super Stickers are converted before the event is emitted, so widgets need no changes;
  - The scraped amount is kept in the new `originalValue` and `originalCurrency` fields of the donate event;
  - Rates are cached per target currency and refreshed once a day;
  - Plugins can convert values through the new `unichat:currency` module;
- Plugins can now emit gift events through `UniChatEvent:Gift{...}`;
- Added the `unichat:utils` module with `random_color_by_seed`;
- YouTube gift events now carry `giftId`, `giftTitle` and a real `giftDescription`:
  - `giftId` and `giftTitle` are derived from the gift asset filename, so the title is an approximation of the real name;
  - `giftDescription` now carries the actual description, or `null` when it can't be read;
  - The default widget's `giftTemplateMessage` field now renders;

### Event contract changes

Breaking for anyone typed against the previous contract. The YouTube and Twitch scrapers keep filling the same fields as before.

- `authorType` is no longer nullable on raid, redemption and gift events;
- `raid.authorId` is no longer nullable. On YouTube it holds a temporary value, as the raid banner carries no channel id;
- `gift.authorProfilePictureUrl` and `redemption.rewardIconUrl` are now nullable;
- `sponsor.tier` comes as `null` on YouTube new member events instead of the channel name; milestone events still carry the real tier;
- Fixed the Lua type definitions: `UniChatEvent:Other` is really `UniChatEvent:Custom`, `raid.viewer_count` is really `viewerCount`, and `clear.platform` had the wrong type;
- Added the missing `UniChatEventUserstoreUpdate` and `UniChatEventCustom` TypeScript interfaces;

### Fixes and Improvements
- Fixed YouTube new member events reporting the channel name as the membership tier;
- Fixed emulated gift events being cached as real (they reappeared when a widget source reconnected);
- Fixed widgets rendering the literal string `null` when an event field was empty;
- Updated icons and splash screen;
- Plugin kick moved to [`unichat-community/unichat-plugin-kick`](https://codeberg.org/unichat-community/unichat-plugin-kick);
- Removed author type `VIP` (detected with `unichat:raw:twitch:vip` flag);
- Fully refactored interface;
- Improved releases and third party licenses loading;
- Fixed gallery assets loading;
- Global shared emotes loads at init;
- Fixed loading of external images (emotes, avatars, etc.) with incomplete addresses;
- Fixed update check cache folder verification;
- Fixed YouTube Super Chat parsing in locales that render the currency symbol after the amount (those events were being dropped);
- Removed the example widget from the widget list;
- Renamed widget files:
  | Before      | Now           |
  |-------------|---------------|
  | `style.css` | `widget.css`  |
  | `main.html` | `widget.html` |
  | `script.js` | `widget.js`   |

### Other Changes
- Changed license from MPL-2.0 to EPL-2.0;
- Locked width and height of main window to `1024x576`;
- Moved docs to another repository;
- Fixed jsonrpc server error parsing;
- Added a content security policy to the application;
- The scraper webview button is now hidden behind a developer setting;
- Scrapers now always run in English on Windows, avoiding events being missed on systems with another language;
- Twitch scraper now blocks ad frames completely;
- Widgets that disconnect are now properly detected and cleaned up;
- Improved scrapers and plugins log identification;
- Restored the `switch` variant of `checkbox` fields in widgets;
- Removed mantine and all react stack, replaced by preact with tailwindcss and self implemented components.
- Replaced the internal web server and http client with lighter alternatives;
- Updated toolchains to:
  - `node` to `24.16.0`;
  - `pnpm` to `10.33.4`;
  - `rust` to `1.95.0`;
